
namespace GameOfLifeWpfBoard.Utils
{
    public enum PatternType { Simple, brick, oscillator, rPentomino };

    public enum mode { StartCycle, StopCycle }

    public enum speed { slow = 3000, medium = 500, fast = 300, faster = 200, fastest = 50 }
}
